super + Return
    alacritty

super + Escape
    pkill -USR1 -x sxhkd

super + alt + {q,r}
    bspc {quit,wm -r}

alt + {_,shift + }F4
    bspc node -{c,k}

alt + space
    rofi -show combi -combi-modes "window,run" -modes combi
